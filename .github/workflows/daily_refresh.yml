name: Daily App Refresh

on:
  schedule:
    # Every day at 6:00 AM UTC (1:00 AM EST)
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  refresh:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4